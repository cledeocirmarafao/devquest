// O Array é uma estrutura de dados que servem pra que a gente consiga armazenar e organizar outros tipos de dados dentro dele;
// Eles são organizados em forma de lista e o Array tem posições e cada um desses dados vai ficar em uma posição, vai ocupar um espaço dentro do Array;
// Ele pode armazenar qualquer tipo de dado numérico, então pode armazenar um tipo boolean, number, string e até mesmo um outro Array;

/*
let filmes = ['Vingadores', 'Mulher Maravilha']
let avaliacoes = [1, 4.6, 5]
*/

// console.log(filmes[0]);


// Acima segue a anatômia do Array; Todo array o índice começa com 0;

//Pra imprimir o Array todo segue o exemplo abaixo;

// console.log(filmes);


// O Array não precisa ser só de tipo string, tipo number, não. Ele pode ser misturado

/*
let descricaoFilmes = ['Vingadores', 4.5, true]

console.log(descricaoFilmes);
*/

// Oque acontece se tentarmos imprimir uma função que não existe no Array? Exemplo:

let filmes = ['Vingadores', 'Mulher Maravilha']

console.log(filmes[2]);


// Também podemos ter Arrays dentro de Arrays, demonstração na próxima aula;